pipeline {
  agent any
  stages {
    stage('Pre-Staging') {
      steps {
        sh 'pwd'
        sh 'ls -lahrt'
        sh 'cd CNE-Practical-Project'
        sh 'pwd'
        sh 'cd infrastructure/jenkins'
        sh 'ls -lahrt'
        sh 'cd ../ansible'
        sh 'ls -lahrt'
        sh 'cat .gitignore'
      }
    }

  }
}